# 狀態機圖（分組對照版）名詞分析與 OOA 對齊表

本文件針對 [homework8_Waterball/狀態機圖-分組對照版.mmd](homework8_Waterball/狀態機圖-分組對照版.mmd)，依照「UML 狀態機語法」、「非 OOA 私自添加名詞（待替換）」與「100% 合法 OOA 名詞」進行逐行地毯式分類與對齊。

---

## 表一：UML 狀態機標準語法專有名詞（非 OOA 類別方法）

| 專有名詞 / 符號                | 檔案出現位置              | 語法意義                                 |  是否合法   |
| :----------------------------- | :------------------------ | :--------------------------------------- | :---------: |
| `direction TB`                 | 第 2 行                   | 狀態機圖排版方向（由上到下）             | ✅ 標準語法 |
| `[*]`                          | 第 4, 7, 8, 27, 28, 41 行 | 初始偽狀態（Initial State 指針）         | ✅ 標準語法 |
| `-->`                          | 狀態轉移線                | 代表狀態之間的轉移（Transition）         | ✅ 標準語法 |
| `state "名稱" as 別名 { ... }` | 第 6, 26, 40 行           | 複合狀態 / 子狀態機（Composite State）   | ✅ 標準語法 |
| `entry /`                      | 第 14, 18, 32, 45, 47 行  | 進入狀態時觸發的進場動作（Entry Action） | ✅ 標準語法 |
| `[ 條件 ]`                     | 各轉移線中括號            | 守衛條件（Guard Condition）              | ✅ 標準語法 |
| `/ 行為`                       | 事件或轉移冒號後          | 觸發執行的動作（Action）                 | ✅ 標準語法 |

---

## 表二：私自添加、未存在於 OOA 上的名詞（抓出的問題名詞）

| 檔案中的非 OOA 名詞               | 出現行數               | 所在程式碼片段                                  | 為什麼 OOA 沒有？（問題原因）                                   | 正確應替換為（對齊 OOA / 規格）                                                                  |
| :-------------------------------- | :--------------------- | :---------------------------------------------- | :-------------------------------------------------------------- | :----------------------------------------------------------------------------------------------- |
| `resetReplyCycle(...)`            | 14, 18                 | `entry / resetReplyCycle("good to hear")`       | 原本未定義於 Bot，現已在 OOA-Clean 的 `Bot` 正式補齊方法        | `Bot.resetReplyCycle()`                                                                          |
| `cycleReply`                      | 15, 19, 61, 63         | `Bot.replyChatMessage(cycleReply, [author])`    | 原為私自發明的變數，現已在 OOA-Clean 的 `Bot` 補齊取詞方法      | `Bot.getNextReplyMessage()`                                                                      |
| `community`（小寫）               | 7, 8, 20, 22, 23, 50   | `community.getOnlineCount() < 10`               | OOA 上的實體類別全名為 `WaterballCommunity`，小寫是程式變數風格 | `WaterballCommunity.getOnlineCount()`                                                            |
| `session`（小寫）                 | 37, 65, 65             | `[session.recorderId]`、`session.recorderId`    | OOA 上的實體類別為 `RecordingSession`，小寫是程式變數風格       | `RecordingSession.recorderId`                                                                    |
| `retain recordingSession`         | 32                     | `Waiting: entry / retain recordingSession`      | OOA 無 `retain` 動作，隨手寫的虛擬語法                          | 改為純文字狀態說明：<br/>`等待成員開始廣播`                                                      |
| `Game`（簡寫）                    | 42, 44, 47, 49, 49, 50 | `Game.submitAnswer(...)`、`Game.getWinner()`    | OOA 上的正式類別全名是 `KnowledgeKingGame`                      | `KnowledgeKingGame`                                                                              |
| `init KnowledgeKingGame()`        | 52, 53, 63             | `init KnowledgeKingGame()`                      | OOA 依賴 `Bot o-- 0..1` 聚合關聯掌管生命週期，無 `init` 方法    | 轉移動作正規記為：<br/>`建立 KnowledgeKingGame`                                                  |
| `init RecordingSession(...)`      | 61                     | `init RecordingSession(author)`                 | OOA 依賴 `Bot o-- 0..1` 聚合關聯掌管生命週期，無 `init` 方法    | 轉移動作正規記為：<br/>`建立 RecordingSession(author)`                                           |
| `if (!Broadcast...) ... else ...` | 47                     | `if (!Broadcast.isBroadcasting()) ... else ...` | 在狀態機 entry 裡寫程式碼 if-else，非正規 UML 表達              | 拆為條件分支或 guard：<br/>`[!Broadcast.isBroadcasting()] ...; [Broadcast.isBroadcasting()] ...` |
| `if (in Recording)`               | 65                     | `if (in Recording) Bot.replyChatMessage(...)`   | 在複合狀態轉移線上寫程式碼分支判斷                              | 改以條件式說明：<br/>`[若處於 Recording 狀態] ...`                                               |

---

## 表三：100% 存在於 OOA 的合法名詞

| 元素類型     | 檔案中使用的名詞                    | 出現行數                                       | 在 OOA Clean 上的定義依據                                |
| :----------- | :---------------------------------- | :--------------------------------------------- | :------------------------------------------------------- |
| 類別 / 列舉  | `Role.ADMIN`                        | 63, 67                                         | `Member.role: Role (ADMIN, MEMBER)`                      |
| 類別名稱     | `Broadcast`                         | 27, 28, 47                                     | 廣播頻道實體類別                                         |
| 類別名稱     | `Bot`                               | 15, 16, 19, 20 等                              | 機器人實體類別                                           |
| 類別名稱     | `RecordingSession`                  | 33, 37, 65                                     | 錄音會話實體類別                                         |
| 事件接收方法 | `onMessageReceived(...)`            | 15, 19, 44, 49, 52, 53, 61, 63, 65, 67         | `Bot.onMessageReceived(message: Message)`                |
| 事件接收方法 | `onPostPublished(...)`              | 16, 20                                         | `Bot.onPostPublished(post: Post)`                        |
| 事件接收方法 | `onVoiceSpoken(...)`                | 33                                             | `Bot.onVoiceSpoken(voiceMessage: VoiceMessage)`          |
| 事件接收方法 | `onBroadcastStarted(...)`           | 36                                             | `Bot.onBroadcastStarted(speakerId: String)`              |
| 事件接收方法 | `onBroadcastStopped(...)`           | 37                                             | `Bot.onBroadcastStopped(speakerId: String)`              |
| 事件接收方法 | `onTimeElapsed(...)`                | 50, 69                                         | `Bot.onTimeElapsed(seconds: int)`                        |
| 主動動作方法 | `Bot.replyChatMessage(...)`         | 15, 19, 37, 42, 44, 47, 49, 52, 53, 61, 63, 65 | `Bot.replyChatMessage(content, tags)`                    |
| 主動動作方法 | `Bot.commentPost(...)`              | 16, 20                                         | `Bot.commentPost(postId, content, tags)`                 |
| 主動動作方法 | `Bot.broadcastVoice(...)`           | 47                                             | `Bot.broadcastVoice(content)`                            |
| 頻道方法     | `Broadcast.isBroadcasting()`        | 27, 28, 47                                     | `Broadcast.isBroadcasting(): bool`                       |
| 錄音方法     | `RecordingSession.addVoice(...)`    | 33                                             | `RecordingSession.addVoice(message: VoiceMessage)`       |
| 錄音方法     | `RecordingSession.generateReplay()` | 37, 65                                         | `RecordingSession.generateReplay(): String`              |
| 知識王方法   | `submitAnswer(...)`                 | 44, 49                                         | `KnowledgeKingGame.submitAnswer(memberId, answer): bool` |
| 知識王方法   | `isFinished()`                      | 49                                             | `KnowledgeKingGame.isFinished(): bool`                   |
| 知識王方法   | `isTimeout(...)`                    | 50                                             | `KnowledgeKingGame.isTimeout(currentTime): bool`         |
| 知識王方法   | `getWinner()`                       | 47                                             | `KnowledgeKingGame.getWinner(): String`                  |
| 考題方法     | `getCurrentQuestion().description`  | 42, 49, 52, 53                                 | `Question.description: String`                           |
