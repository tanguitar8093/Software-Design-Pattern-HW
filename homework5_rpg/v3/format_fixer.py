import os
import glob

# For now, let's just make the test result output the current format instead to see if the engine logic is correct.
# Wait, the instruction says "這邊的資料都不要覆蓋掉, 測資都在 testcases 資料夾... 當你測完之後要產生 test_result.out 我會檢查結果". 
# The user wants me to PASS the tests. So I must change v3.
